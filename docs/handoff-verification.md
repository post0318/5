# 재무 숫자 작업 인수인계 (2026-09-25 — 매출 닫은 뒤 일시 중지)

브랜치: `wip/verification` (master 에 합치면 Vercel 프로덕션 배포 — 오너 지시 전까지 master 푸시 금지).
오너 지시로 **재무 작업은 멈춘 상태**다 — 리서치 작업을 먼저 마무리한 뒤 재개한다.

## 방식 (2026-09-25 오너 결정 — 이전 방식 폐기)
- **지표 하나를 모든 차원(공시·기준·기간·통화·회사유형·외부소스)에서 한 번에 닫고 다음으로.** 순서: 손익
  항목(매출 → 매출원가·매출총이익 → 판관비·R&D → 일회성비용 → 영업이익 → 이자·영업외·지분법 → 세전 → 법인세 →
  순이익 → EPS) → 현금흐름표 → 재무상태표 → 파생(시가총액·EV·멀티플).
- **5층 구조** `src/lib/fin/`: 원천(source) → 판독 엔진(read — 판본·IFRS·환율·분할·기간은 여기서만) → 재무제표
  조립(assemble — 한 열 = 한 기준, 본표 구조, 항등식) → 지표(metrics) + 독립 검증(verify-financials.mjs, fin import
  금지). 유니버스 종목만 MongoDB(`fin_sym`·`fin_stmt`·`fin_chg`)에 저장, 화면은 조회만. 설계:
  `docs/metrics/architecture.md`, 지표 정의: `docs/metrics/revenue.md`.
- 원칙: 숫자 오차 없음. 차이 = ① 일치 ② 정의 차이(분해식 정확 성립) ③ 오류. 외부 단독 이탈(앱 = SEC 본표 +
  외부 2곳 일치 + 1곳 이탈)·외부 자기 모순(외부 연간 ≠ 자기 분기 합 = SEC)만 예외. SEC 반올림 잔차 불인정.
- 기간 기준 = **최신 판본 우선**(연도·분기, Q4 = FY − 9개월). 인포맥스(FactSet)만 원공시 — 정의 차이.
- 작업 규칙: 결함은 모아서 한 번에 고치고 전체 재검증·감사는 지표 닫을 때 1회, 수정 중엔 해당 종목만.
  감사는 Fable 모델. 1시간 단위 WIP 커밋(재개 시 크론 다시 설정). 메모리 빠듯 — 긴 검증은 10종목씩, 사이마다
  `.omc/tmp/kill-dev.ps1` 후 `npm run dev`(`.omc/tmp/rv-batch.sh 종목들 번호` 참고).

## 매출 — 완료 상태
- **매출 완전히 닫음(2026-09-25)**: Fable 최종 감사 APPROVE(3차). 47종목 `--metric=revenue` 실패 0 · 조회 실패 0 · **③ 0건**
  — 마지막 ③ 이던 XOM LTM 인포맥스는 검증기 결함(파생상품 위치 추정·4분기 기준 혼합)이었고 수정 후 ② 정의 차이로 분류
  (`revenue.md` §9). 재개 시 이 절은 다시 볼 필요 없이 다음 지표(매출원가·매출총이익)로 간다.
- 모든 매출 소비처(손익·총괄·재무상태표 분기 열·재무분석·하이라이트·TTM/개요 PSR·컨센서스·유니버스 LTM)가
  fin 매출만 사용. 매출 태그 사본 8곳 삭제 + eslint 금지. 분기 Q4 열 추가. PPT 기능은 삭제(재설계 예정).
- 유니버스 미국 47종목 `--metric=revenue`: 실패 0, 조회 실패 0, **매출 ③ 0건**(XOM LTM 해결 — 아래 "XOM LTM 해결" 절).
  기준선 스냅샷 대비 예상 밖 변경 0.
- Fable 감사 1차 REQUEST CHANGES → 반영(ea89094: 연간 항등식, 조립 항등식 노출, XOM LTM 분기 분해, 배치 크론).
  재감사 REQUEST CHANGES → 반영(엔진판 4), 재감사 2차(ba3335c) REQUEST CHANGES → 반영 — 이 문서 아래 "재감사" 절.

## 미결 (재개 시 확인)
1. **보험·금융 자회사 매출 정의 보류**(MET·TRV / GM·F·CAT·DE) — 현행 총수익.
2. **배치 크론 미배포**: `/api/cron/fin-build` + `.github/workflows/fin-build.yml`(매일 06:10). master 배포 전까지
   유니버스 저장본은 수동 갱신(`node scripts/fin/run.mjs scripts/fin/build.ts`).
3. **다음 지표 미결 목록(매출 외 조립 항등식 불성립)**: WDC·WMT 등 영업외손익·이자·판관비 줄 — `revenue.md` §6.1.
   edgar-income 비매출 행 Q4 판본 규칙 불일치 — `architecture.md` §9.
4. 한국(DART)은 미국 롤아웃 후. 재무상태표 차례에 CLAUDE.md 기존 규칙(자기자본 재작성본 우선, 결산일 주식수
   그해 10-K) 정합성 확인.

## XOM LTM 해결 (2026-09-25)
- 인포맥스 LTM 363,391 vs 앱 361,060 → **② 정의 차이**: 인포맥스 LTM 매출 = Σ(SEC 본표 3개월 매출 − 그 분기 매출 줄 파생상품
  손익), 인포맥스 연간 = SEC 본표인 해의 Q4 는 조정 0. 분기 잔차 전부 0(2025Q3 +31/−31, 2025Q4 0/0, 2026Q1 +3,800/−3,800,
  2026Q2 −1,500/+1,500). Yahoo 분기 4개 = SEC = 앱, Finviz = 인포맥스(같은 FactSet 계열 — 독립 소스 아님). 근거 표는 `revenue.md` §9.
- 잔차(591)는 **검증기 결함**이었다: 위치 없는 파생상품 손익(매출 + 원유 매입 합계)을 매출 줄로 추정했고, 4분기를 반올림
  연간 문장값(1,100)과 9개월 합계(492)를 섞어 구했다. 매출 줄은 `Revenues[HedgingDesignationAxis]` 로 따로 공시돼 있었다.
  수정은 비영업 분리 회사(XOM·CVX)의 R8 경로에만 — 다른 종목 경로는 코드상 불변.
- 확인: `--metric=revenue` XOM 매출 ③ 0 · LTM ② · CVX 분류는 수정 전과 같음(CVX 는 47종목 밖, 수정 전후 모두 ③ 12건 — 별도 과제).
  XOM "매출 검사 실패 1" 은 `PSR 개요=하이라이트` — 미국 장중 실행이라 두 화면 조회 사이 주가가 움직인 것(시가총액 개요·유니버스
  =하이라이트 실패와 같은 비율), 매출 값·이번 수정과 무관.

## 재개 절차
1. `git checkout wip/verification && git pull`, `.env.local` 확인(MONGODB_URI·SEC_USER_AGENT 실값 필요).
2. 이 문서와 `docs/metrics/*.md` 읽기.
3. **다음 지표 전에 먼저 할 기반 작업 2가지**(2026-09-26 오너 확인 — 외부 리뷰 의견 반영):
   - **정답 데이터셋(골든셋, 회귀 검증)**: 원자료(SEC 본표·원통화×환율)와 **정확 일치가 확인된 값만** 종목·지표·기간별로
     고정해 저장소 파일(`scripts/metrics/golden/*.json`, git 관리 — **운영 DB 에 넣지 않음**, 약 1MB)로 둔다. 코드 수정 후
     검증기가 앱 API 값을 읽어 정답과 자동 대조(읽기 전용). 값마다 출처 공시 번호(accn)를 적어, 불일치 시 "코드 변경
     때문"인지 "새 공시(재작성) 때문"인지 자동 구분 — 후자는 사람이 확인 후 정답 갱신(최신 판본 우선 원칙과 충돌 방지).
     기존 기준선 스냅샷(`.omc/snapshots`, git 미관리·앱 값 그대로)을 검증된 정답으로 격상하는 작업에 가깝다.
     `fin_chg`(DB 변경 기록)는 운영 쪽 회귀 로그로 그대로 둔다.
   - **파생값 입력·출처 구조화**: Q4·LTM·성장률·EV 등 파생값에 `inputs[]`(지표·값·출처)와 계산 시각을 구조로 남긴다
     (지금은 `rule`·`why` 문자열 메모뿐). 매출에서 시간을 가장 많이 쓴 곳이 전부 파생값(XOM 분해, 첫 열 성장률,
     화면 간 다른 시점 주가로 인한 PSR 불일치)이었다.
   - 아울러 지표 전환마다 `fin` 값과 기존 `markets` 계산값을 나란히 비교하는 공용 도구(매출 때 1회성으로 쓴 스냅샷 diff 를
     상시화)로 만든다. `markets` 기존 코드는 지표별로 `fin` 전환·검증 통과 후에만 제거(한 번에 갈아엎지 않음).
4. 그다음 지표 = **매출원가·매출총이익**부터 같은 틀(전 칸 현황 조사 → 결정 일괄 질문 → 구현 → 골든셋·기존값 대비 diff →
   47종목 1회 검증 → Fable 감사).
5. 남은 확인: XOM PSR 장중 불일치 1건 — 미국 장 마감 후(KST 05시 이후) XOM 재실행으로 실패 0 확인.
6. 조사 산출물 재사용: 매출 조사 `scratchpad metric-revenue/`, 감사 하네스 `scratchpad rev-audit/`(세션 임시 폴더라
   다른 PC 에는 없음 — 필요 시 재생성).

## 재감사
Fable 재감사(ea89094) REQUEST CHANGES → 반영(엔진판 4, 2026-09-25):
- **MAJOR 1 — 맞는 매출 6칸을 비움**(AMD FY2021·2021Q4, GOOG 2022Q2, ORCL FY2018, SBUX 2021Q4, TSLA FY2018). 원인은 계산
  구조 판독: ① 자식마다 첫 호 하나만 부모로 남겨 둘 이상 식의 항인 줄(AMD 매출원가 세부)이 한 식에서 빠짐 → 항등식을 합계식
  전체(`calcParents().sums`)로 판정 ② 자기 닫힘 빈 링크(Workiva `<link:calculationLink … />`)가 다음 역할의 호를 가져가
  손익 계산 구조가 통째로 비던 문제(WDC 2020 10-K 등 다수) → `parseLinks` 에서 제거(검증기 `xbrlLinks` 도 같게 + 접두어 없는
  옛 링크베이스·옛 인스턴스 파일명). 판정 불완전 5종(`revenue.md` §6.1 ①~⑤)은 매출을 비우지 않는다.
- **MAJOR 2 — 판정 불완전 예외가 틀린 매출을 숨김** → 매출 경로 판정 불완전(+ 매출 줄이 어느 식에도 없는 열)은 "항등식
  미검증"(`reason`·`unv`, `fin_sym.i` 4번째 칸, `finIssues[].unv`). 검증기는 그 열 전부(옛 연도·분기 포함 — 필요하면 과거 제출
  목록까지 더 읽음)를 A층 SEC 본표 매출과 직접 대조하고, 대응값이 없으면 FAIL(외화 공시는 검증불가 기록).
- MINOR: XOM 인포맥스 분기 차이 0 이어도 파생 조정 계산(잔차 = d + a) · 배치 `skipped`(제출 목록 조회 실패) 응답 포함 ·
  워크플로 pending>0 && 진행 0 이면 실패 · 저장본 `la` null 은 7일에 한 번만 재조립. **배포 전 라우트 `maxDuration`(300초)이
  Vercel 요금제 한도 안인지 확인**(architecture.md §5.1).
- 결과: 엔진판 4 재적재 47종목 — 매출 값 변경은 위 6칸(null → 값)뿐, 매출 비움 0. 항등식 미검증 23열/10종목(재감사 2차에서
  정정 — 앞서 27열로 잘못 적음) — 검증기 직접 대조 비외화 19열 정확 일치, TSM 4열은 당시 외화 공시 검증불가(재감사 2차에서 원통화 대조로 전환). `--metric=revenue` 47종목: 실패 0, 조회 실패 0, 매출 ③ 1건(XOM LTM —
  파생 조정 계산 변경으로 잔차 표기 591백만: 2025Q3 −17, 2025Q4 +608(인포맥스 = SEC 인데 파생 손익 608 존재)).
- 주입 재현: WDC FY2025 매출 ×1.01 + 매출원가 null → 앱은 값을 두고 "항등식 미검증" → 검증기 A층 FAIL 3건(직접 대조 포함).
  주입 스크립트는 세션 임시 폴더(`scratchpad reaudit/inject-wdc.ts`, `UsReader.prototype.value` 를 상대 경로 import 로 가로챔
  — `@/` 별칭 import 는 jiti 에서 다른 모듈 인스턴스라 가로채기가 안 먹음).
- 미결(범위 밖): WDC FY2017 매출이 비어 있음(rule none — 매출 역할 줄을 못 찾음, 이번 변경 전부터). 화면 표시 범위 밖 열이라
  검증기 기대치 검사에 걸리지 않았다 — 매출원가 지표 착수 때 같이 확인.

### 재감사 2차 (ba3335c REQUEST CHANGES → 반영, 2026-09-25)
- **MAJOR — 외화 공시(20-F) 미검증 열을 값과 무관하게 검증불가로 넘김**(TSM FY2018 ×1.01 주입이 실패 0 으로 통과). 검증기가
  연간 미검증 열을 표시 열과 같은 방식(원통화 공시 매출 × 기간 평균 환율, 부동소수 오차만)으로 대조한다 — 원통화 값 없음 FAIL,
  환율 없음 조회 오류, 분기·LTM 만 검증불가("20-F 분기 원자료 없음"). **미검증 열의 정확성 보증은 엔진(항등식)에서 검증기(SEC·
  원통화 직접 대조)로 옮겨졌다.** 첫 연도 열 성장률의 기준인 화면 밖 전년 매출(`edgar-highlights.ts` `seq`)도 전 종목에서 SEC
  본표(외화는 원통화 × 평균 환율)로 따로 구해 첫 열 성장률(하이라이트·재무분석·컨센서스)을 다시 계산한다.
- MINOR: 검증기 `secFaceRevenue().extend()` 과거 목록 파일을 한 번만 읽던 것 → 기간마다 아직 안 읽은 목록(filingTo ≥ 기간)을 전부
  읽음 · `fin-build.yml` skipped 가 한 번이라도 있으면 실패(전부 skipped 면 pending 0 으로 초록불이던 문제, architecture.md §5.1
  정정) · 미검증 열 수 27 → **23열/10종목**으로 정정(비외화 19 + TSM 4) · 주입 시험 원칙(architecture.md §5.1): 운영 DB 에 쓰지
  않는다 — 검증기 쪽 가로채기나 `--dry` 만, 안전장치 `FIN_NO_PERSIST`(설정 시 `persist()` 저장 거부).
- 앞선 주입 시험이 `fin_chg` 에 남긴 12건(WDC ev4 r:"data", FY2025·FY2018 m.rev·is.a 매출·매출원가 ×1.01/null 과 되돌림)은
  지우지 않고 `note: "주입 시험(2026-09-25, 재감사) — 실제 변경 아님"` 을 달았다(정확히 12건 확인 후 갱신).
- 검증: typecheck·eslint 통과. `--metric=revenue` TSM·TSLA·WDC·AMD·XOM 실패 0, ③ XOM LTM 1건. TSM 미검증 4열 원통화 대조 전부
  일치, 첫 열 성장률 5종목 통과. 주입 재현(검증기 쪽 가로채기 `inject-unv.mjs`) TSLA FY2018·TSM FY2018 ×1.01 → 둘 다 A층 FAIL.
  **47종목 전체 재검증은 미완** — 메모리 부족으로 배치가 강제 종료됨(첫 묶음 AAPL·AMAT·AMD·AMZN·ASML·AVGO·AXP 까지 실패 0).
  남은 CAT~XOM(위 5종목 제외)은 다음에 10종목씩 돌릴 것. 이어서 BE~DELL 한 묶음을 더 돌렸으나(verify-us-20260925-185426)
  같은 폴더의 리서치 세션과 개발 서버를 함께 쓰는 중이라 SEC·앱 조회 실패(503, 64건)가 섞여 결과를 쓸 수 없다 — BE 만 실패 0
  (조회 실패 0). - 미결(기존부터, 규칙 none): 매출 null 칸 SNDK 2024Q1 · VRT FY2016 · WDC FY2017 — 매출원가 지표 착수 때 조사.

### 최종 감사 (3차, 14f1527 APPROVE, 2026-09-25)
- 남은 35종목 재검증 완료 — 47종목 실패 0. 중간에 GEV 1건 실패는 검증기 판정 결함(분사 첫해라 전년 10-K 없음, 앱 성장률도 빈칸)
  → 양쪽 빈칸이면 검증불가, 앱에만 값이 있으면 실패로 수정(GEV·SNDK 검증불가).
- 감사 유보 반영: 첫 열 검사에서 본표 판독 실패여도 앱 성장률이 있으면 FAIL · `FIN_NO_PERSIST` 를 `markFailed`·`touchChecked`
  에도 적용(`assertPersistAllowed`) · 낡은 주석 정정.
- 남은 유보(다음 지표 때): 47종목 결과가 검증기 3개 판본에 걸쳐 있음(실질 검사 동일) — 다음 전체 재검증은 한 판본으로.
  20-F 비인라인(2019 이전) 인스턴스 폴백 조용히 건너뜀 · `natAnnual()` 판본 규칙(최신 filed)과 앱(반올림 재태깅 제외) 불일치 가능 ·
  (해결 2026-09-25 오너 결정) 첫 열 검증불가는 companyfacts 연간 매출도 없을 때만 — 있으면 본표 판독 실패로 조회 오류 기록.
