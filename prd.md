# PRD — 글로벌 종목 리서치 대시보드

- 문서 버전: 2026-09-01
- 상태: 초안 (스캐폴딩 전)
- 관련 문서: `CLAUDE.md` (개발 가이드)

---

## 1. 개요

유니버스로 관리하는 종목에 대해 **재무제표 · 최근 공시 · 관련 뉴스 · 멀티플**을
한 화면에서 조회하는 웹 대시보드.

- 기존 참고 서비스: `sss.globalmarket.einformax.co.kr`
  - 미국/중국 ETF 정보를 탭으로 조회
  - "종목분석 > 개요"에서 종목 입력 시 회사정보 · 재무제표 표시
- 개선 방향
  - 개별 종목 조회 → **유니버스 단위 통합 조회**
  - 재무 + 공시 + 뉴스 + 멀티플을 한 화면에
  - 일부 데이터는 외부 사이트 딥링크로 연계
- **배포 시나리오: 개인용으로 시작, 나중에 팀/대외 확장 가능성** (상세 §4.0)

## 2. 대상 시장

| 시장 | 상태 |
|------|------|
| 한국 | 포함 |
| 미국 | 포함 |
| 일본 | 포함 |
| 중국 · 유럽 | 제외 (추후 확장 여지로만 보류) |

- 상단 탭: **한국 / 미국 / 일본**
- 중국 제외: 공식 오픈 API 부실
- 유럽 제외: 2027-07 ESAP 가동 전까지 무료 공식 소스 부족

## 3. 기술 스택

| 영역 | 선택 |
|------|------|
| 프레임워크 | Next.js (App Router) + TypeScript |
| 스타일 | Tailwind CSS + shadcn/ui, 다크/라이트 모드 |
| 테이블 | TanStack Table |
| 차트 | Recharts (필요 시 lightweight-charts 추가) |
| 데이터 패칭 | TanStack Query + Next.js fetch 캐싱 |
| DB | Postgres (Vercel Marketplace — Neon 등) |
| 날짜 | date-fns |
| 시세·컨센서스 | `yahoo-finance2` (npm, Python 불필요) — 개인용 한정 |
| 배포 | Vercel (Cron 포함) |

- 외부 금융 API 호출은 **서버(Route Handler)에서만** — 키 은닉, CORS 회피, 캐싱, rate limit 관리
- 시장별 로직은 어댑터로 분리: `lib/markets/{kr,us,jp}/`

## 4. 데이터 아키텍처

### 4.0 배포 시나리오 = 개인용 시작, 확장 가능성 (핵심 전제)

- **현재: 개인용.** 본인 1인 사용, 공개 배포·팀 공유·수익화 없음.
  → 무료 API의 "personal / non-commercial" 조건 안에 들어옴.
  → **L4 포워드 컨센서스도 인앱 렌더링 허용** (개인용 한정).
- **나중에: 팀 내부 또는 대외로 확장 가능.**
  → 확장 시점에 라이선스 위반이 되는 소스가 생김 (§4.3, §11).
  → **모든 외부 소스는 어댑터 인터페이스 뒤에 격리**, 소스 교체가 설정/파일 교체 수준이 되도록 설계.
- 설계 원칙: **"개인용으로 빠르게, 확장 지점은 명확하게"**

### 4.1 레이어별 소스

| 레이어 | 데이터 | 한국 | 미국 | 일본 | 개인용 렌더링 |
|--------|--------|------|------|------|--------------|
| **L1** | 기업개황 · 공시 · 재무제표 | OpenDART | SEC EDGAR | EDINET API v2 | 인앱 |
| **L2** | EOD(일별) 시세 | Stooq (주) / yahoo-finance2 (보조) | 〃 | 〃 | 인앱 |
| **L3** | 트레일링 멀티플 | 자체 계산 (L1+L2) | 자체 계산 | 자체 계산 | 인앱 |
| **L4** | 포워드 멀티플 · 컨센서스 | yahoo-finance2 `quoteSummary` (개인용) + 딥링크 병행 | 〃 | 〃 | **인앱 + 딥링크** |
| **L5** | 뉴스 | yahoo-finance2 `search` / 규제기관 공시 + 딥링크 | 〃 | 〃 | 인앱 목록 + 딥링크 |

- **L1**: 배포 시나리오와 무관하게 항상 공식 무료 API. 라이선스 이슈 없음.
- **L2**: Stooq가 주. `yahoo-finance2`(npm, Python 불필요)로 `.KS`/`.KQ`/`.T` 커버 보조.
- **L4**: `yahoo-finance2`의 `quoteSummary` 모듈(`earningsTrend`, `financialData`,
  `recommendationTrend`, `defaultKeyStatistics`)로 forward EPS/매출, forward PER, 목표주가,
  투자의견을 **인앱 표시**. 원본 사이트(stockanalysis 등) 딥링크는 상세 뷰로 병행.
- **L5**: `yahoo-finance2` `search`/뉴스, 규제기관 공시 RSS. 원문은 딥링크.

### 4.2 결정 사항

- EOD만 사용. **실시간 시세는 요구사항 아님.**
- L2 한국 커버리지가 부족하면 공공데이터포털 "금융위원회_주식시세정보" / KRX OPEN API로 폴백 (§4.4).
- L4·L5는 인앱 표시하되 **원본 딥링크도 항상 같이** 제공 (검증·상세 확인용).
- 사내 인포맥스 API가 확보되면 L2·L4·L5를 그쪽으로 교체.

### 4.3 확장 시 재검토 필요 (개인용 → 팀/대외 전환 시)

전환 시점에 아래 소스는 **인앱 사용 중단 → 딥링크 전환 또는 정식 라이선스** 필요:

| 소스 | 개인용 | 팀 내부 | 대외 서비스 |
|------|--------|---------|-------------|
| OpenDART / SEC EDGAR / EDINET | ✅ | ✅ | ✅ (그대로 유지) |
| Stooq | ✅ | △ (조건 확인) | △ |
| yahoo-finance2 / yfinance | ✅ (개인·비상업) | ✖ | ✖ |
| Finnhub / FMP / Polygon 무료 | ✅ (개인) | ✖ | ✖ |
| stockanalysis / MarketScreener (스크래핑) | ✖ (딥링크만) | ✖ | ✖ |
| FnGuide 크롤링 | ✖ (딥링크만) | ✖ | ✖ |

- 전환 시 대체 경로: ① 사내 인포맥스 API ② 정식 라이선스(LSEG/FactSet/S&P, 한국 FnGuide API) ③ 딥링크.
- 크롤링은 어떤 시나리오에서도 금지 (FnGuide `robots.txt Disallow: /`, 각 사이트 ToS).

### 4.4 폴백 소스

| 상황 | 폴백 |
|------|------|
| Stooq 한국 커버리지 부족 | 공공데이터포털 "금융위원회_주식시세정보" 또는 KRX OPEN API (공식·무료, T+1) |
| 일본 재무 정리가 EDINET XBRL 파싱으로 과중 | J-Quants Light (¥1,650/월, 5년치) 유료 플랜 검토 |

### 4.5 수집 · 저장 전략

- **매일 배치 수집 → Postgres 저장** (L1 공시·재무, L2 시세, L4 컨센서스 스냅샷).
  외부 API rate limit 회피 + 히스토리 축적 + 조회 속도.
- **Vercel Cron**: 시세는 각 시장 마감 후, 공시는 주기 폴링(예: 30분), 컨센서스는 일 1회.
- 온디맨드 조회는 **DB 우선**, 캐시 미스만 실시간 API 호출.
- L3 멀티플은 조회 시점 계산 (또는 일 1회 스냅샷 저장).

### 4.6 딥링크 대상 (초안)

인앱 데이터의 **상세·검증용 병행 링크**. 확장 시 L4·L5 주력이 됨.

| 시장 | L4 포워드 컨센서스 | L5 뉴스 |
|------|-------------------|---------|
| 한국 | 네이버페이 증권(종목분석), 컴퍼니가이드 | 네이버페이 증권 뉴스, 인포맥스/한경 |
| 미국 | stockanalysis.com(`/forecast`), Yahoo Finance(`/quote/{T}/analysis`), MarketScreener | Yahoo Finance, stockanalysis.com |
| 일본 | Yahoo Finance(`/quote/{code}.T/analysis`), MarketScreener, IR BANK(irbank.net) | IR BANK, Yahoo Finance Japan |

- MarketScreener는 무료 열람에 일부 페이지 제한 있음 (사용자 계정 필요할 수 있음)
- 최종 URL 패턴은 §12 확정 후 고정

## 5. 기능 요구사항

### 5.1 시장 탭
- 한국 / 미국 / 일본 전환
- 각 탭은 동일한 하위 구조(종목분석, 유니버스 통합 뷰) 공유

### 5.2 종목분석
- 종목(티커/종목코드) 입력 → 존재 검증 → 조회
- 회사정보(기업개황) + 재무제표 표시 (§6)

### 5.3 유니버스 통합 뷰
유니버스 등록 종목 전체를 대상으로 한 화면에 집계:
- 재무제표 요약
- 최근 공시사항
- 관련 뉴스 (인앱 목록 + 원문 딥링크)
- 멀티플 (트레일링 = 자체 계산 인앱, 포워드 = yahoo-finance2 인앱 + 원본 딥링크 병행)

### 5.4 유니버스 관리 (별도 화면)
- **개별 등록**: 티커/코드 입력 → 존재 검증 → 추가
- **일괄 업로드**: CSV/Excel 파일 업로드 또는 텍스트 붙여넣기
- **관리**: 목록 조회, 삭제, 그룹·태그 분류(시장/섹터 등), 활성·비활성 토글
- **접근 제어**: 간단한 인증 (방식 §12 미결)

### 5.5 외부 연계 (딥링크)
- 종목별 URL 템플릿으로 외부 사이트 새 탭 이동
- 대상: 포워드 컨센서스, 뉴스, (한국) 컴퍼니가이드/네이버금융 등
- 공시 원문도 각 규제기관(DART/EDGAR/EDINET) 딥링크

## 6. 재무제표 표현 규칙

- **연간 / 분기 구분**: 탭 또는 토글로 전환
- **원본 표현 그대로 유지**
  - 소스의 계정과목명 · 단위 · 부호를 임의 재가공/환산하지 않음
  - 한국은 `fnlttSinglAcntAll`(전체 재무제표) 사용 (원본 계정 보존)
- **마이너스 값**: 빨간색 텍스트 (괄호 표기 `(1,234)` 옵션 검토)
- **주요 항목 배경색 강조**
  - 대상 계정(매출액/영업이익/당기순이익 등)은 **설정값으로 관리**
  - 정확한 강조 계정 목록: §12 미결
- 숫자 우측 정렬 + `tabular-nums`

## 7. 숫자 · 통화 표시 규칙

- **천 단위 구분**: 콤마(`,`) — 예: `1,234,567`
- **외화(USD, JPY 등)**: 소수점 **둘째 자리까지**, **버림(trunc, 반올림 아님)**
  - 예: `1234.567 → 1,234.56`
- **원화(KRW)**: **정수**, 버림(trunc)
  - 예: `1234.9 → 1,234`
- 공통 유틸 `formatCurrency(value, currency)` 로 전 화면 일관 적용
- 버림 구현 시 부동소수점 오차 주의 (정수 스케일링/문자열 처리 검토)
- 음수 / null / 0 표시 방식: §12 미결

## 8. 날짜 처리 요구사항 (필수 방어)

- **입력 중간값 파싱 금지**
  - `2027` 타이핑 중 `0002`, `020` 등 미완성 값이 즉시 파싱/조회되지 않도록
  - 연도 4자리 완성 시 · blur 시 · 명시적 "조회" 트리거 시에만 실행
  - `onChange`마다 조회 금지 → debounce 또는 명시 트리거
- **연도 범위 가드**: 허용 범위(예: 1990 ~ 2100) 밖이면 조회 안 함
- **무한 루프 방지**
  - 날짜 계산 루프(`while (d < end)`)에 상한 카운터 필수
  - `useEffect` 의존성에 Date 객체 직접 사용 금지 → timestamp/문자열로 정규화
- `date-fns` 사용, 직접 Date 산술 최소화

## 9. 멀티플

- **필요 항목**: 트레일링 + 포워드 둘 다
- **트레일링 (인앱, 자체 계산)**: 주가 + 재무제표로 계산
  - PER = Price / EPS(TTM), PBR = Price / BPS, PSR, EV/EBITDA, 배당수익률 등
  - 라이선스 문제 없음 (모든 배포 시나리오)
- **포워드 (개인용: 인앱 + 딥링크 병행)**
  - `yahoo-finance2` `quoteSummary`로 forward EPS/매출·forward PER·목표주가·투자의견 인앱 표시
  - **개인용 한정.** 팀/대외 확장 시 인앱 중단 → 딥링크 또는 정식 라이선스 (§4.3)
  - 원본 사이트(stockanalysis 등) 딥링크는 항상 병행

## 10. 디자인

- 세련된 금융 대시보드 스타일
- 정보 밀도 높되 정돈된 그리드, 카드 기반 섹션, 여백 활용
- 뉴트럴 베이스(그레이/슬레이트) + 포인트 컬러 절제
- 등락 색상: 시장 관행 (미국식 녹색↑·적색↓ / 한국식 선택 옵션)
- 타이포: 숫자 `tabular-nums`, 본문/데이터 폰트 구분
- 다크 / 라이트 모드 지원
- 차트 · 테이블도 동일 디자인 토큰 공유
- 필요 시 `design` 캔버스로 화면별 목업 선작업

## 11. 데이터 소스 평가 (2026-09 기준, 참고)

### 채택

| 소스 | 용도 | 비고 |
|------|------|------|
| OpenDART | 한국 L1 | API 키 무료, `fnlttSinglAcntAll` |
| SEC EDGAR | 미국 L1 | companyfacts / submissions, 키 불필요, User-Agent 필수 |
| EDINET API v2 | 일본 L1 | 이메일+전화로 키 발급, 무료 |
| Stooq | 한·미·일 L2 (EOD 시세) | 키 불필요, 이용조건 관대. 한국 커버리지 확인 필요 |

### 폴백 / 유료 옵션

| 소스 | 판정 |
|------|------|
| 공공데이터포털 "금융위원회_주식시세정보" / KRX OPEN API | 한국 EOD 폴백 (공식·무료, T+1) |
| J-Quants Light (¥1,650/월) | 일본 구조화 재무·시세. 무료 플랜은 12주 지연이라 부적합, 저가 유료는 검토 대상 |

### 개인용 허용 / 확장 시 배제

| 소스 | 개인용 | 팀·대외 | 비고 |
|------|--------|---------|------|
| yahoo-finance2 (npm) | ✅ 인앱 | ✖ | Yahoo ToS 개인·비상업. L2 보조 + L4 인앱 표시에 사용. 원본은 LSEG 라이선스 |
| yfinance / yahooquery (Python) | ✅ | ✖ | 위와 동일. JS(yahoo-finance2) 우선이라 채택 안 함 |
| Finnhub 무료 | ✅ | ✖ | 명시적 개인용/비상업. 한·일 시세·재무 미커버 |
| Polygon.io 무료 | ✅ | ✖ | 미국 전용, 분당 5회, 15분 지연 |
| Financial Modeling Prep 무료 | ✅ | ✖ | 미국 전용, 일 250회, 최종 사용자 표시·재배포 불가 |
| Business Quant 무료 | △ | ✖ | 포워드 컨센서스 조회는 되나 ToS 미공개·벤더 불투명 → 보류 |
| J-Quants 무료 | ✖ | ✖ | 12주 지연 → 최근 데이터 부적합 (Light 유료는 폴백 후보) |
| 컴퍼니가이드(FnGuide) 크롤링 | ✖ | ✖ | robots 전면 차단, 유료 라이선스 → 딥링크만 |
| stockanalysis / MarketScreener 스크래핑 | ✖ | ✖ | ToS 위반 → 딥링크만 |

### 포워드 컨센서스(L4) 확인 결과 (2026-09)

- **무료 조회는 기술적으로 가능** (yfinance/yahoo-finance2 / Business Quant / Finnhub / 여러 웹사이트에서 2026 추정치 확인됨).
- 원본은 **LSEG·FactSet·S&P 등 유료 컨센서스 제공사** 데이터. 무료 API는 "개인용/비상업" 조건으로 재제공.
- 웹사이트는 **열람·딥링크는 자유, 스크래핑은 ToS 위반**.
- **현재 배포 시나리오 = 개인용** → `yahoo-finance2`로 **인앱 표시 허용** (§4.0).
- **팀/대외 확장 시** → 인앱 중단, 대체 경로: ① 사내 인포맥스 ② 정식 라이선스(LSEG/FactSet/S&P, 한국 FnGuide API) ③ 딥링크.
- 딥링크는 개인용에서도 상세 뷰로 병행. 대상은 §4.6.

## 12. 미결 사항

- [ ] 사내 인포맥스 API 존재·스펙 (시세 / 뉴스 / 포워드 컨센서스) — 확보 시 해당 레이어 인앱 전환
- [ ] Stooq 한국(KRX/KOSDAQ) 커버리지·품질 실측 → 부족 시 공공데이터포털로 폴백 결정
- [ ] 강조할 재무 계정 항목 리스트 (시장별)
- [ ] 음수 / null / 0 숫자 표시 방식
- [ ] 유니버스 관리 화면 인증 방식
- [ ] 재무제표 단위 표기 정책 (원본 단위 유지 vs 통일)
- [ ] 유니버스 그룹/태그 분류 체계
- [ ] 뉴스 "관련성" 판단 기준 (종목-뉴스 매핑)
- [ ] L4/L5 딥링크 **URL 패턴** 확정 (대상 사이트는 §4.6 초안 완료)
- [ ] MarketScreener 무료 열람 제한 범위 확인

## 13. 진행 상황

### 완료 (v0.1 스캐폴드)
- [x] Next.js 16 + TS + Tailwind 4 + shadcn/ui 스캐폴딩
- [x] 데이터 소스 어댑터 인터페이스 (`src/lib/markets/types.ts`, `registry.ts`)
- [x] L1 미국 어댑터 (SEC EDGAR — 회사정보·재무제표·공시, 키 불필요)
- [x] L1 한국/일본 어댑터 골격 (키 발급 후 구현 대기 — NotConfiguredError + 딥링크 동작)
- [x] L2 시세 (Stooq 주 → yahoo-finance2 폴백)
- [x] L3 트레일링 멀티플 자체 계산 (현재 최근 연간 기준)
- [x] L4 포워드 컨센서스 (yahoo-finance2, 개인용) + 딥링크 병행
- [x] L5 뉴스 딥링크
- [x] 공통 유틸 `format.ts` (콤마·trunc), `dates.ts` (연도 방어·루프 상한)
- [x] DB: Drizzle + libSQL(SQLite), `universe_items` 스키마 + 마이그레이션
- [x] 유니버스 관리 (개별 등록 / 일괄 업로드 파서 / 활성 토글 / 삭제)
- [x] 화면: 시장 탭 · 종목분석(개요/재무제표/공시) · 유니버스 통합 뷰 · 유니버스 관리
- [x] 다크/라이트 모드, tabular-nums, 마이너스 빨간색, 강조행 배경색

### 완료 (v0.2)
- [x] 한국(OpenDART) 어댑터 — corpCode 매핑(zip 파싱) + 기업개황 + 전체 재무제표(연/분기) + 공시목록
- [x] 종목 검색 (이름·코드) — 한국 DART corpCode / 미국 EDGAR 티커맵 / 일본·공통 Yahoo search
- [x] 검색 자동완성 UI (debounce, 키보드 네비)
- [x] 한국 시세 Yahoo `.KS/.KQ` 자동 폴백

### 완료 (v0.3)
- [x] 일본(EDINET) 어댑터 골격 — Edinetcode 목록(회사정보·검색, 키 불필요) + 공시목록 스캔(키 필요)
- [x] 일본 종목명 검색 (일/영/코드)

### 다음
1. 일본 재무제표 — EDINET 문서 CSV/XBRL 파싱 (배치 수집 인덱스 필요, §4.5)
2. 정확한 TTM 멀티플 (EDGAR 분기 start/end 구간 판별)
3. 한국 시세 정확도 — 공공데이터포털/KRX 폴백 추가 (§4.4)
4. 배치 수집 → DB 캐시 + Vercel Cron (§4.5)
5. §12 미결 사항 확정 (강조 계정, 음수/null 표기, 인증, 그룹/태그 UI)
6. 재무제표 차트(Recharts)
