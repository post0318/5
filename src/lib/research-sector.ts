/**
 * 산업분석 탭 업종 필터의 표준 섹터 분류(오너 지시, 2026-09-19 — "산업분석
 * 분류값을 너무 엉터리도 해두었네.. 섹터 분류값은 한국지수와 미국지수
 * 분류값을 섞어서 만들어줘"). 기존엔 수집기마다 제각각인 원문 stockName을
 * 그대로 드롭다운에 나열해(예: "IT"/"IT하드웨어"/"IT산업"/"IT소프트웨어"가
 * 전부 따로, "산업"/"시장" 같은 수집기 기본값·"DS Defense Daily"/"TGIF" 같은
 * 게시판 라벨까지 섞여 나옴, 실측) 필터가 사실상 무의미했다.
 *
 * 표준 목록은 실제 지수 체계에서 그대로 가져온다(오너 지시 — "한국지수와
 * 미국지수 분류값을 섞어서") — `lib/weekly/sectors.ts`(다른 세션이 먼저
 * 만든 "주요 섹터 이슈" 기능, KRX 코스피 200/코스닥 150 섹터지수 + 미국
 * SPDR Select Sector ETF 실측 확인)와 같은 소스. 오너가 직접 KOSPI200
 * 11개+KOSDAQ150 7개+미국 SPDR 11개 목록을 제시하며 "이 분류값을 벗어날
 * 것은 없어보인다"고 확인 — 근접 동의어(생활소비재/필수소비재,
 * 경기소비재/자유소비재)는 드롭다운에서 중복으로 보이지 않게 하나로
 * 합치고, 코스피가 하나로 묶은 "에너지/화학"·코스피 고유의 "중공업"은
 * 실제 연구 콘텐츠 라벨이 뚜렷이 갈려(정유·화학·철강 vs 조선·방산)
 * 그대로 분리 유지했다 — 완전한 1:1 재현은 아니지만 오너가 확인한 3개
 * 지수 체계 "밖으로 벗어나는" 새 카테고리는 만들지 않았다.
 *
 * 분류는 원문 stockName·title 키워드 매칭이다(`classifyResearchTopic()`과
 * 같은 방식·같은 한계 — 완전하지 않음). 매칭 안 되는 원문(수집기 기본값
 * "산업"/"시장", 게시판 라벨 "TGIF"/"Cash is King" 등, 실측 다수)은
 * null — 업종 드롭다운에서만 빠지고 "전체"/다른 조회에는 그대로 남는다.
 */

export const SECTOR_LABELS = [
  "정보기술",
  "커뮤니케이션서비스",
  "금융",
  "헬스케어",
  "산업재",
  "중공업",
  "건설",
  "에너지",
  "소재",
  "경기소비재",
  "필수소비재",
  "유틸리티",
  "부동산",
] as const;

export type SectorLabel = (typeof SECTOR_LABELS)[number];

// 순서가 우선순위다 — 위에서부터 먼저 매칭된 규칙을 쓴다(예: "건설리츠"는
// 부동산 규칙이 건설보다 먼저 와야 리츠로 분류됨).
const SECTOR_RULES: [SectorLabel, RegExp][] = [
  ["부동산", /부동산|리츠|REIT/i],
  ["중공업", /조선|방산|방위산업|항공우주/],
  ["소재", /철강|화학|소재|2차\s?전지|배터리|Battery/i],
  // "원자재"는 여기 없었다가 빠졌다 — 해운(벌크선 원자재 운송)·구리 등 소재
  // 리포트에도 흔히 섞여 나와 산업재(해운)가 에너지로 잘못 분류되는 문제가
  // 있었다(오너 지적, 2026-09-22 — "산업분석에서 에너지업종에 해운이
  // 들어가있다"). 에너지 리포트는 아래 다른 키워드로 이미 충분히 잡힌다.
  ["에너지", /에너지|정유|석유|가스전/],
  ["헬스케어", /제약|바이오|헬스케어|의료|디지털헬스/],
  ["금융", /은행|증권|보험|금융|카드\/?결제/],
  [
    "정보기술",
    /\bIT\b|\bAI\b|아이티|반도체|소프트웨어|인터넷|게임|파운드리|소부장|하드웨어|전자상거래|테크|디지털자산|클라우드|사이버보안|로보틱스|통신기기|통신장비/i,
  ],
  ["커뮤니케이션서비스", /통신서비스|통신|미디어|엔터|콘텐츠/],
  ["건설", /건설|건자재/],
  ["유틸리티", /유틸리티|전력\s?솔루션|전력\s?생산/],
  ["산업재", /산업재|운송|물류|해운|항공(?!우주)|기계|전력기기/],
  ["경기소비재", /자동차|화장품|유통|여행|레저|가전|라이프스타일|모빌리티/],
  ["필수소비재", /음식료|담배|농업/],
];

export function classifySector(doc: { stockName?: string | null; title?: string | null }): SectorLabel | null {
  const hay = `${doc.stockName ?? ""} ${doc.title ?? ""}`;
  for (const [label, re] of SECTOR_RULES) {
    if (re.test(hay)) return label;
  }
  return null;
}
